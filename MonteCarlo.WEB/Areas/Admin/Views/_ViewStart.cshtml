@{
    // Predeterminado del area: panel administrativo.
    // Las vistas de autenticacion lo sobreescriben con
    // Layout = "_AuthLayout" en su propio bloque @{ }.
    Layout = "_AdminLayout";
}
